[package]
name = "sistema_ro_frontend"
version = "0.1.0"
description = "Shell desktop Tauri do sistema_ro"
authors = ["Codex"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.0.0" }

[dependencies]
tauri = { version = "2.0.0" }

[features]
default = []
